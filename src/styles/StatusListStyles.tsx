import {StyleSheet} from 'react-native';

export const StatusListStyle = StyleSheet.create({
  container: {
    flex: 1,
    padding: 4,
  },
  loadingIndicator: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
